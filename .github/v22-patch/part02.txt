tual?'Live routing active':'Preview mode'}</span>${FAMILY_VIEW?'<span class="liveChip">Read-only</span>':''}</div></div>
- <div class="livePanel"><div class="liveControls"><label>Park<select onchange="liveDayPark=this.value;liveFocusArea='';localStorage.removeItem('dw27_live_area');renderToday();if(isSelectedDayToday())loadLiveDayData(this.value)">${parks.map(p=>`<option${p===park?' selected':''}>${escAttr(p)}</option>`).join('')}</select></label><label>Day<select onchange="currentDay=Number(this.value);liveFocusArea='';renderAll();if(isSelectedDayToday())loadLiveDayData((DAYS[currentDay].parks||[])[0])">${DAYS.map((x,i)=>`<option value="${i}"${i===currentDay?' selected':''}>${escAttr(x.label)} · ${escAttr(x.title)}</option>`).join('')}</select></label></div><div class="routeModes"><button class="routeModeBtn${liveRouteMode==='plan'?' active':''}" onclick="setLiveRouteMode('plan')">Follow plan</button><button class="routeModeBtn${liveRouteMode==='nearby'?' active':''}" onclick="setLiveRouteMode('nearby')">Optimize nearby</button></div>${liveRouteMode==='nearby'?`<select class="liveAreaSelect" onchange="setLiveFocusArea(this.value)"><option value="">Any area in ${escAttr(park)}</option>${(PARK_AREAS[park]||[]).map(a=>`<option value="${escAttr(a)}"${normalizeAreaForPark(a,park)===normalizeAreaForPark(liveFocusArea,park)?' selected':''}>${escAttr(a)}</option>`).join('')}</select>`:''}<div class="liveHeroRow" style="margin-top:9px"><button class="liveRefresh primary" onclick="loadLiveDayData('${park}',true)">${liveDayLoading?'Loading…':actual?'Refresh live waits':'Preview today’s park data'}</button>${MAPS[park]?`<a class="tinyAction" href="${MAPS[park]}" target="_blank" rel="noopener">Park map</a>`:''}<a class="tinyAction" href="https://disneyworld.app.link/MyDisneyExperienceEN-US" target="_blank" rel="noopener">My Disney Experience</a></div>${!actual?'<div class="liveStale">Wait times shown here are today’s park data, not a prediction for your 2027 date. On your trip day this screen becomes your live navigator.</div>':''}</div>
+ <div class="livePanel"><div class="liveControls"><label>Park<select onchange="liveDayPark=this.value;liveFocusArea='';localStorage.removeItem('dw27_live_area');renderToday();if(isSelectedDayToday())loadLiveDayData(this.value)">${parks.map(p=>`<option${p===park?' selected':''}>${escAttr(p)}</option>`).join('')}</select></label><label>Day<select onchange="currentDay=Number(this.value);liveFocusArea='';renderAll();if(isSelectedDayToday())loadLiveDayData((DAYS[currentDay].parks||[])[0])">${DAYS.map((x,i)=>`<option value="${i}"${i===currentDay?' selected':''}>${escAttr(x.label)} · ${escAttr(x.title)}</option>`).join('')}</select></label></div><div class="routeModes"><button class="routeModeBtn${liveRouteMode==='plan'?' active':''}" onclick="setLiveRouteMode('plan')">Follow plan</button><button class="routeModeBtn${liveRouteMode==='nearby'?' active':''}" onclick="setLiveRouteMode('nearby')">Optimize nearby</button></div>${liveRouteMode==='nearby'?`<select class="liveAreaSelect" onchange="setLiveFocusArea(this.value)"><option value="">Any area in ${escAttr(park)}</option>${(PARK_AREAS[park]||[]).map(a=>`<option value="${escAttr(a)}"${normalizeAreaForPark(a,park)===normalizeAreaForPark(liveFocusArea,park)?' selected':''}>${escAttr(a)}</option>`).join('')}</select>`:''}<div class="liveHeroRow" style="margin-top:9px"><button class="liveRefresh primary" onclick="loadLiveDayData('${park}',true)">${liveDayLoading?'Loading…':actual?'Refresh live waits':'Preview live data now'}</button>${MAPS[park]?`<a class="tinyAction" href="${MAPS[park]}" target="_blank" rel="noopener">Park map</a>`:''}<a class="tinyAction" href="https://disneyworld.app.link/MyDisneyExperienceEN-US" target="_blank" rel="noopener">My Disney Experience</a></div>${!actual?'<div class="liveStale">Wait times shown here are today’s park data, not a prediction for your 2027 date. On your trip day this screen becomes your live navigator.</div>':''}</div>
  <div class="livePanel"><h2>Where you are</h2>${here?`<div class="anchorRow"><div class="anchorMain"><div class="anchorName">${escAttr(here.name)}</div><div class="anchorMeta">Last completed / selected location</div></div></div>`:'<div class="muted">Mark an attraction completed or tap “I’m here” on a recommendation and routing will use that location.</div>'}</div>
  <div class="livePanel"><h2>Next fixed plan</h2>${anchor?`<div class="anchorRow"><div class="anchorTime">${escAttr(from24Time(anchor.t))}</div><div class="anchorMain"><div class="anchorName">${escAttr(anchor.name)}</div><div class="anchorMeta">${escAttr(anchor.meta||'')}</div></div></div>`:'<div class="liveEmpty">No fixed reservation, showtime or Lightning Lane time ahead.</div>'}</div>
  <div class="livePanel"><h2>${actual?'Best next move':'Planned options'}</h2>${pd&&pd.error?`<div class="liveEmpty">Live data unavailable: ${escAttr(pd.error)}</div>`:recs.length?recs.map(r=>`<div class="liveRec"><div><div 