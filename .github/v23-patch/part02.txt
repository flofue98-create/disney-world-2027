iv class="routeIds">${ropeNames.map(n=>`<span class="strategyChip">${n}</span>`).join('')}</div>`:''}${ll.length?`<div class="routeLine"><b>Lightning Lane targets:</b> ${ll.map(id=>BY_ID[id]?.name).filter(Boolean).join(' • ')}</div>`:''}</div>`:`<div class="strategyPreview"><b>Strategy will follow this day as plans lock</b><div class="small">Rope-drop order, Lightning Lane priorities and geographic routing will update as the park day becomes more defined.</div></div>`}</div>`;
+ const review=renderDayAgentReview(d);
+ el.innerHTML=`<div class="agentHub"><div class="agentHubTop"><div><div class="eyebrow">Personal Travel Agent</div><h2>${d?.label||'Selected day'} strategy</h2><div class="small">Planning months out now • dynamic routing on park day</div></div><button class="agentHubBtn editorOnly" onclick="switchScreen('dashboard')">Travel Agent Center</button></div><div class="agentMiniGrid"><div class="agentMini"><div class="agentMiniLabel">Dining window</div><div class="agentMiniValue">${fmtMilestoneDate(dining.date)}</div><div class="agentMiniMeta">Current 60-day rule</div></div><div class="agentMini"><div class="agentMiniLabel">Lightning Lane</div><div class="agentMiniValue">${fmtMilestoneDate(lane.date)}</div><div class="agentMiniMeta">Current 7-day Resort rule</div></div><div class="agentMini"><div class="agentMiniLabel">Park</div><div class="agentMiniValue">${(d?.parks||[]).join(' + ')||'Flexible day'}</div><div class="agentMiniMeta">Route follows selected day</div></div><div class="agentMini"><div class="agentMiniLabel">Showtimes</div><div class="agentMiniValue">2026 estimate mode</div><div class="agentMiniMeta">August 2026 patterns until 2027 times publish</div></div></div>${rope?`<div class="strategyPreview"><b>${rope.title}</b><div class="small">${rope.note}</div>${ropeNames.length?`<div class="routeIds">${ropeNames.map(n=>`<span class="strategyChip">${n}</span>`).join('')}</div>`:''}${ll.length?`<div class="routeLine"><b>Lightning Lane targets:</b> ${ll.map(id=>BY_ID[id]?.name).filter(Boolean).join(' • ')}</div>`:''}</div>`:`<div class="strategyPreview"><b>Strategy will follow this day as plans lock</b><div class="small">Rope-drop order, Lightning Lane priorities and geographic routing will update as the park day becomes more defined.</div></div>`}${review}</div>`;
 }
+let bbbWhatIfOpen=false;
+function findMealSlotOnDay(d,mealId){for(let bi=0;bi<(d.blocks||[]).length;bi++){const b=d.blocks[bi];if(b.meal===mealId)return{bi,b,slot:'meal'};if(b.meal2===mealId)return{bi,b,slot:'meal2'}}return null}
+function renderDayAgentReview(d){
+ if(!d)return'';
+ const bbbBi=(d.blocks||[]).findIndex(b=>b.reservation==='mk-bbb'),crt=findMealSlotOnDay(d,'mk-crt');
+ if(bbbBi<0||!crt)return `<div class="agentReview good"><div class="agentReviewTitle">Agent review ready</div><div class="agentReviewMeta">As you add fixed dining, appointments, showtimes and Lightning Lane windows, I’ll flag timing conflicts and better sequencing here.</div></div>`;
+ const b=d.blocks[bbbBi],bbbTime=getReservationTime(b)||parsedBlockTimes(b).start||'',crtTime=getMealTime(crt.b,crt.slot)||parsedBlockTimes(crt.b).start||'17:30';
+ const bbbMin=timeToMinutes(bbbTime),crtMin=timeToMinutes(crtTime),target='15:15',targetMin=timeToMinutes(target);
+ let cls='',title='',meta='';
+ if(!bbbMin||bbbMin<13*60){
+  title='Agent suggestion: test an afternoon Bibbidi Bobbidi Boutique';
+  meta=`The current plan uses the makeover early. A ${from24Time(target)} target would protect the high-value morning ride window and keep Liliana’s hair and makeup much fresher for Cinderella’s Royal Table at ${from24Time(crtTime)}. Disney says the transformation can take up to 90 minutes, so this leaves about ${Math.max(0,Math.round((crtMin-targetMin)/60*10)/10)} hours before dinner.`;
+ }else if(bbbMin<=15*60+30 && crtMin-bbbMin>=105){
+  cls='good';title='Agent check: this makeover timing is well aligned';meta=`Bibbidi Bobbidi Boutique is now close enough to Cinderella’s Royal Table to keep the makeover fresh while still leaving a reasonable cushion before ${from24Time(crtTime)}.`;
+ }else{
+  cls='warn';title='Agent timing warning: the makeover may be too close to dinner';meta=`Disney says Bibbidi Bobbidi Boutique can take up to 90 minutes. With Cinderella’s Royal Table at ${from24Time(crtTime)}, I would target roughly 3:00–3:30 PM rather than pushing the appointment much later.`;
+ }
+ const preview=bbbWhatIfOpen?`<div class="whatIfFlow"><div class="whatIfRow"><div class="whatIfTime">8:30 AM</div><div>Use Early Entry for Fantasyland priorities instead of spending the best low-wait window at the makeover.</div></div><div class="whatIfRow"><div class="whatIfTime">11:35 AM</div><div>Mickey’s Magical Friendship Faire <span class="estimateBadge">2026 estimate</span></div></div><div class="whatIfRow"><div class="whatIfTime">12:15 PM</div><div>Quick-Service lunch in Fantasyland.</div></div><div class="whatIfRow"><div class="whatIfTime">2:00 PM</div><div>Festival of Fantasy Parade <span class="estimateBadge">2026 estimate</span></div></div><div class="whatIfRow"><div class="whatIfTime">3:15 PM</div><div>Bibbidi Bobbidi Boutique. Budget up to 90 minutes, then castle-area photos.</div></div><div class="whatIfRow"><div class="whatIfTime">5:30 PM</div><div>Cinderella’s Royal Table — makeover still fresh for the princess dinner.</div></div><div class="agentReviewMeta"><b>Tradeoff:</b> the current 2:35–5:00 Liberty Square / Adventureland stretch would need to move later or to the second Magic Kingdom day. This preview does not change your itinerary.</div></div>`:'';
+ return `<div class="agentReview ${cls}"><div class="agentReviewTitle">${title}</div><div class="agentReviewMeta">${meta}</div><div class="agentReviewActions"><button onclick="bbbWhatIfOpen=!bbbWhatIfOpen;renderPlanningAssistant()">${bbbWhatIfOpen?'Hide what-if':'Preview what-if'}</button><button onclick="openQuickSheet('reservation',${bbbBi})">Open BBB timing</button></div>${preview}</div>`;
+}
+
 function renderStrategyCenter(){
  const el=document.getElementById('dashboardStrategy');if(!el)return;
  const useful=DAYS.filter(d=>ROPE_DROP[d.date]||llPriorityForDay(d).length);
@@ -1938,7 +1961,7 @@
  const ids=b.ids||[],idx=ids.indexOf(id),s=status(id,currentDay);quickTitle(x.name,x.area||'Attraction');
  const targets=(d.block